<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>LAMP - Login</title>

    <link
      rel="icon"
      type="image/png"
      href="/images/transperent-lamp-cd.png"
      sizes="32x32"
    />
    <link rel="stylesheet" href="/css/login-working.css" />
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Archivo+Black&family=Bungee+Tint&family=Chokokutai&family=Comfortaa:wght@300..700&family=Coral+Pixels&family=Dongle&family=Gaegu&family=IM+Fell+Double+Pica:ital@0;1&family=Jersey+15&family=Kirang+Haerang&family=Lacquer&family=Lexend+Deca:wght@100..900&family=Rubik+Dirt&family=Special+Elite&family=Syne+Mono&family=Tiny5&display=swap');
    </style>
  </head>

  <body>
    <div class="album-scroller-container">
      <div class="album-scroller" id="albumScroller">
        <% if (albums && albums.length > 0) { %> <% for (let i = 0; i < 2; i++)
        { %> <% albums.forEach(function(album) { %>
        <div class="album-grid-tile">
          <img
            src="<%= album.images[0]?.url || '/images/default-album.png' %>"
            alt="Album Cover"
          />
        </div>
        <% }) %> <% } %> <% } %>
      </div>
    </div>

    <!-- 🔧 LOGIN FORM - ensure this is here -->
    <div class="signin-content">
      <div class="login-box">
        <h1>/LAMP</h1>
        <h2>Welcome Back</h2>
        <form action="/users/login" method="POST">
          <input type="text" name="username" placeholder="Username" required />
          <input
            type="password"
            name="password"
            placeholder="Password"
            required
          />
          <button type="submit">Sign In</button>

          <% if (error) { %>
          <div class="error"><%= error %></div>
          <% } %>
        </form>
        <p class="signup-link">
          Don’t have an account?
          <br /><a href="/users/signup">Sign up here</a>
        </p>
      </div>
    </div>
  </body>
  <script></script>
</html>
